<h2>Write Message</h2>

<%= semantic_form_for current_user.messages.build(:recipient_id => GLOBAL) do |f| %>
	
	<%= f.inputs do %>

		<%= f.input :content %>
		<%= f.input :recipient_id, :as => :hidden %>
	
  <% end %>

	<%= f.buttons do %>
		<%= f.commit_button %>
	<% end %>
	
<% end %>
